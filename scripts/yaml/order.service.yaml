apiVersion: v1
kind: Service
metadata:
  labels:
    app: aksworkshop
    component: order
  name: order
spec:
  ports:
  - port: 80
    name: http
  selector:
    app: aksworkshop
    component: order
