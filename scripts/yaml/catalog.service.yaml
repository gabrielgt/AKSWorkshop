apiVersion: v1
kind: Service
metadata:
  labels:
    app: aksworkshop
    component: catalog
  name: catalog
spec:
  ports:
  - port: 80
    name: http
  selector:
    app: aksworkshop
    component: catalog
